# WORK_STATUS.md

세션별 진행 상황 기록. 안정적인 프로젝트 지침은 `CLAUDE.md` 참고.

## 최근 세션 작업 목표
- 2026-07 세션: 가족 클라우드 동기화 도입부터 시작해, 실카메라 인증·용돈(EXP=원) 시스템·연속완료 배수·부모 PIN 보안·학습기록·PWA 아이콘·안내 페이지 제작까지 순차 진행
- 사용자(엄마)는 UI/UX 디자이너이지만 빠른 진행을 위해 컬러·폰트·레이아웃 시스템은 아직 정리하지 않음 — 추후 사용자가 직접 디자인을 다시 잡을 예정(데이터와 코드는 분리되어 있어 화면 교체 시 기존 데이터 영향 없음)

## 구현 완료 사항 (모두 커밋·배포 완료, 미반영 변경 없음 — 2026-07-22 기준)
- Supabase 가족 공유 동기화 (family_data 테이블, RLS + SECURITY DEFINER RPC 2개로만 접근)
- 실제 날짜 기반 74일 일정 + 공휴일 자동 제외(광복절/대체공휴일, 추석연휴)
- 성장(레벨·XP·배지) 시스템을 실제 학습 기록 기반으로 전면 개편 (하드코딩 목데이터 제거)
- 주간 계획 화면: 아이가 요일별로 직접 과목 체크 (기본 추천값 + 즉시 반영 + 토스트, 완료한 과목은 체크 잠금)
- 미션 완료 흐름: 과목선택 → 브리핑 → 집중타이머(최소 20분 또는 과목 전체시간 중 짧은 쪽 채워야 완료 가능) → 실제 카메라로 사진 인증(이전엔 가짜 UI였음) → 보상(EXP)
- 학습 기록(사진+공부시간) 저장 및 뷰어 화면, 항목별 수동 삭제(EXP·레벨엔 영향 없음)
- EXP 값 10배 스케일링(1 EXP = 1원, 레벨 테이블도 동일 비율로 조정해 밸런스 유지) + 연속 완료 주(週) 배수 보너스(2/4/6/8주 → ×2/×3/×4/×5, 다음 주부터 적용)
- 용돈 요청/지급 플로우 — 요청 시 부모님께 알림, 지급 확인은 "부모" 기기에서만
- 아이/부모 기기 역할 구분 — 최초 접속 시 선택, 로컬 저장(클라우드 동기화 안 함). "부모" 전환은 4자리 PIN 필요(해시로 Supabase에 가족 공유 저장), PIN 변경 가능
- 저장 공간 부족 시 알림센터에 경고 알림(이전엔 console.warn만 있었음)
- 앱 아이콘 + PWA manifest(홈 화면 추가 시 standalone 모드)
- 타이포그래피/색상 대비 개선, 하단 네비 활성 상태 재설계
- 정적 안내 페이지 2개 — `/guide.html`(아이용 사용법), `/about.html`(기획~배포 회고 + 운영 매뉴얼), 앱 설정 화면에서 링크 제공
- 다수 버그 수정: 완료된 과목 재선택 시 오작동 토스트, 오래된 "42일 프로젝트" 문구 잔재, 데이터 초기화 버튼 일부만 초기화되던 문제, 오늘이 시작 전(D-1)일 때 홈에 아무것도 안 뜨던 날짜 경계 버그, 상단 네비 타이틀이 우측 아이콘과 겹치던 문제

## 최근 변경된 주요 파일 (변경 이유)
- `src/app/App.tsx` — 위 기능들 전부 이 파일에서 구현
- `public/guide.html`, `public/about.html` — 처음엔 Claude Artifact로 만들었으나 로그인 필요 문제로 `public/`에 옮겨 앱과 같은 도메인에서 서빙하도록 전환
- `public/manifest.webmanifest`, `public/icon-*.png` 등 — 기존엔 파비콘/매니페스트 전무했음, PWA 지원 추가
- `package.json` — pnpm lockfile 불일치 해결을 위해 불필요한 `pnpm.overrides` 제거, `packageManager` 필드 고정

## 실행한 테스트 및 결과
- 검증 완료(Playwright, 시스템 Chrome): 실카메라 사진 촬영, 최소 학습시간 게이트(비활성화→활성화 전환), 연속 완료 배수 계산 및 보상 반영, 용돈 요청→알림→지급 전체 플로우, 부모/아이 기기 PIN 인증(정답/오답), PIN 변경, 학습기록 삭제(EXP 불변 확인), 저장공간 부족 경고 알림 발생, 크로스 디바이스 동기화(두 브라우저 컨텍스트로 같은 family_id 공유 확인)

## 미해결 문제
- GitHub→Vercel 자동 배포 미연결 — 사용자가 나중에 요청 예정
- 저장 공간이 실제로 임계치에 가까워졌을 때의 자동 대응(예: 자동 정리)은 의도적으로 구현 안 함 — 수동 삭제 + 경고 알림으로 대체(사용자와 합의된 결정)
- 디자인 시스템(컬러/타이포/레이아웃) 정리는 착수 전 — 사용자가 디자이너로서 추후 직접 진행 예정

## 다음에 수행할 작업
- (사용자 요청 시) GitHub↔Vercel 자동 배포 연결
- (사용자 요청 시) 디자인 시스템 재정립 반영
- 그 외 추가 요청은 미정 — 최근 세션은 소개/가이드 페이지(`guide.html`, `about.html`) 작성 및 앱 내 연결로 마무리됨
